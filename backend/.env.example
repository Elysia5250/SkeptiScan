# OpenAI Compatible API 配置
# 留空则使用 Mock 模式（无需真实 API Key 即可演示）
# 运行时可从前端 UI 配置，优先级：UI > 环境变量 > 默认值
OPENAI_API_KEY=
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
EXTRA_PROMPT=

# 数据库路径
DATABASE_URL=sqlite:///./data/analyses.db