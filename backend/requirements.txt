# FastAPI + 服务器
fastapi
uvicorn
python-multipart

# 数据库
sqlalchemy

# 环境变量
python-dotenv

# HTTP 请求 / OpenAI SDK
httpx
openai

# 图片处理
Pillow

# HTML 解析（页面正文提取）
lxml

# Playwright 网页截图
# 安装浏览器: playwright install chromium
playwright

# OCR 文字提取
# 使用 PyTesseract（轻量，需安装 tesseract-ocr 系统包）
#   macOS: brew install tesseract tesseract-lang
#   Ubuntu: apt install tesseract-ocr tesseract-ocr-chi-sim
# 也可换 PaddleOCR（中文更准，但更重）
#   pip install paddlepaddle paddleocr
pytesseract